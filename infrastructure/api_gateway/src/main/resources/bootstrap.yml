spring:
  application:
    name: service-gateway
  cloud:
    nacos:
      config:
        server-addr:
        group: ONLINE_EDUCATION
        namespace:
        file-extension: yaml
        extension-configs:
          - data-id: Nacos.yml
            group: ONLINE_EDUCATION
            refresh: true
          - data-id: Sentinel.yml
            group: ONLINE_EDUCATION
            refresh: true